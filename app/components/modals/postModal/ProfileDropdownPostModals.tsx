'use client';

/*import {Button, Dropdown, DropdownItem, DropdownMenu, DropdownTrigger, Avatar} from "@nextui-org/react";
import { useEffect } from "react";
interface ProfileDropdownPostModalProps {
    socialNetworks: String[] ;
    stateSocialNetwork: String;
    setSocialNetwork: (network: String) => void;
}
const ProfileDropdownPostModal: React.FC<ProfileDropdownPostModalProps> = ({
    socialNetworks,
    stateSocialNetwork,
    setSocialNetwork
}) => {

    return ( 
        <Dropdown>
            <DropdownTrigger>
                <div className="flex flex-row gap-2 relative cursor-pointer">
                    <div className="h-full w-full absolute top-0 left-0 z-[12]"></div>
                    <Button className="rounded-full h-12 z-0">
                        <Avatar className="-ml-2" src="https://i.pravatar.cc/150?u=a042581f4e29026024d" />
                        {stateSocialNetwork}
                    </Button>
                </div>
            </DropdownTrigger>
            <DropdownMenu>
                { socialNetworks.map((network, index) => {
                    return (
                        <DropdownItem key={index} onPress={()=> {setSocialNetwork(network);}}>{network}</DropdownItem>
                    )
                })}
            </DropdownMenu>
        </Dropdown>
    );
}
 
export default ProfileDropdownPostModal;*/